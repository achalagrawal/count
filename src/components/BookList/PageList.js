import React from 'react';

function PageList(props) {
    const { book, user } = props;
    console.log(book);
    console.log(user);
    return(
    <div>
        See console
    </div>
    )
}

export default PageList;